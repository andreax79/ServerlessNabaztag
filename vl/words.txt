!              ( value addr -- )                        Store a value into memory at the given address
$REPEAT        ( s n -- s )                             Repeat string s n times
'              (immediate) ( -- xt )                    Get the next word from the dictionary and push it
*              ( n1 n2 -- product )                     Multiplies
*              ( n1 n2 n3 -- (n2*n1)                    / Multiplies and Divides /n3 )
*              ( n1 n2 n3 -- remainder quotient )       /MOD Multiplies and Divides and Modulus
+              ( n1 n2 -- sum )                         Adds
+!             ( n addr -- )                            Adds a number to the contents of the address
+LOOP          ( runtime ) ( increment loop-addr -- )   Add increment to loop index and repeat until limit crossing
+LOOP          (immediate) ( -- )                       End a counted loop with variable step from stack
,              ( x -- )                                 Store x at HERE and advance data-space pointer
-              ( n1 n2 -- difference )                  Subtracts
->             (immediate) ( --  )                      Record depth and contents of stack
.              ( n -- )                                 Display n
.S             ( -- )                                   Copy and display the values currently on the data stac
.TASKS         ( -- )                                   List the words in the dictionary
/              ( n1 n2 -- quotient )                    Divides
/MOD           ( n1 n2 -- remainder quotient )          Divides and Modulus
0<>            ( n -- flag )                            True if top number is not zero
0=             ( n -- flag )                            True if top number zero
1+             ( n -- n+1 )                             Increments top of stack by 1
1-             ( n -- n-1 )                             Decrements top of stack by 1
2!             ( x1 x2 addr -- )                        Store two cells into memory
2*             ( n -- n*2 )                             Multiplies top of stack by 2
2+             ( n -- n+2 )                             Increments top of stack by 2
2-             ( n -- n-2 )                             Decrements top of stack by 2
2/             ( n -- n/2 )                             Divides top of stack by 2
2>R            ( x1 x2 -- ) ( R: -- x1 x2 )             Move two items to return stack
2@             ( addr -- x1 x2 )                        Fetch two cells from memory
2DROP          ( x1 x2 -- )                             Drop the top two cells from the stack
2DUP           ( x1 x2 -- x1 x2 x1 x2 )                 Copy the top two cells on the stack
2OVER          ( x1 x2 x3 x4 -- x1 x2 x3 x4 x1 x2 )     Copy the second pair of cells to the top of the stack
2R>            ( -- x1 x2 ) ( r: x1 x2 -- )             Retrieve two items from return stack
2R@            ( -- x1 x2 ) ( r: x1 x2 -- x1 x2 )       Copy two items from return stack to data stack
2SWAP          ( x1 x2 x3 x4 -- x3 x4 x1 x2 )           Exchange the top two cell pairs
:              ( -- )                                   Begin a new word definition
::             ( list x -- list' )                      Add an element to the front of a list
;              ( -- )                                   End a word definition
<              ( n1 n2 -- flag )                        True if second word less than top word
<0             ( n -- flag )                            True if top number less than zero
<=             ( n1 n2 -- flag )                        True if second word less than or equal to top word
<>             ( n1 n2 -- flag )                        True if top two words are not equal
=              ( n1 n2 -- flag )                        True if top two words equal
>              ( n1 n2 -- flag )                        True if second word greater than top word
>0             ( n -- flag )                            True if top number greater than zero
>=             ( n1 n2 -- flag )                        True if second word greater than or equal to
>NUMBER        ( s -- n )                               Convert string s to number n
>R             ( x -- ) ( R: -- x )                     Move top item to return stack for temporary storage
?              ( addr -- )                              Prints the contents of the address, followed by one space
?DO            ( runtime ) ( limit start leave-addr -- ) Enter a DO...LOOP runtime frame or skip loop if start==limit
?DO            (immediate) ( limit start -- )           Begin a counted loop that is skipped when start equals limit
?DUP           ( x -- 0 | x x )                         Duplicate top of stack if not zero
?JMP           ( cond addr -- )                         Jump if zero
@              ( addr -- value )                        Fetch a value from memory at the given address
ABORT          ( i * x -- ) ( R: j * x -- )             Empty the data stack and the return stack
ABS            ( n -- abs(n)                            Absolute value  )
AGAIN          (immediate) ( -- )                       End a loop Compile an unconditional jump back to the matching BEGIN
AI-EAR-STEPS   ( current target -- n )                  steps along the shortest path
AI-EARS-ARRIVED ( -- flag )
AI-EARS-TRAVEL-MS ( -- ms )                             generous estimate of the longest ear travel
AI-READ-EAR-HEALTH ( -- )
AI-SHORTEST-EAR-DIR ( current target -- dir )
AI-SLEEP>TEXT  ( flag -- text )
AI-VALID-EAR-TARGET? ( target -- flag )
AI-VALID-LED-COLOR? ( color -- flag )
AI-WEATHER>TEXT ( code -- text )
ALLOCATE-CELL  ( -- addr )                              Allocate a cell from memory and return its address
AND            ( x1 x2 -- x3 )                          x3 is the bit-by-bit logical "and" of x1 with x2
BEGIN          (immediate) ( -- )                       Begin a loop Store the current position in the return stack
BETWEEN        ( n low high -- flag )
BL             ( -- char )                              Character value for a space
CALC-SURPRISE  ( -- )                                   calculate next surprise time
CALC-TAICHI    ( -- )                                   Calculate next taichi time
CASE           (immediate) ( -- )                       Begin a case statement
CELL+          ( addr1 -- addr2 )                       Add one cell to an address
CELLS          ( n -- n )                               Multiply n by the size of a cell
CLEAR-INFO     ( -- )                                   Clear all information
CONSTANT       (immediate) ( x "<spaces>name" -- )      Define a constant with the given name and value
COUNT          ( list/str -- n )                        Get the length of a list or a string
CR             ( -- )                                   Display CR
CREATE         ( "<spaces>name" -- )                    Create a new dictionary entry that pushes its private data-field address
CRONTAB        ( -- )                                   Crontab
DAYTIME        ( -- ) (UTC)                             Display the current time
DEFINED?       (immediate) ( "<spaces>name" -- flag )   Check if a word is defined in the dictionary
DEPTH          ( -- count )                             Count number of items on stack
DO             ( runtime ) ( limit start leave-addr -- ) Enter a DO...LOOP runtime frame
DO             (immediate) ( limit start -- )           Begin a counted loop
DOES>          (immediate) ( -- )                       Split current defining word into a CREATE prelude and run-time behavior
DROP           ( n --  )                                Discard top of stack
DUP            ( n -- n n )                             Duplicate top of stack
EAIQ>INFO      ( n -- n )                               Convert European Air Quality Index to info code
EAR-BROKEN?    ( ear -- flag )                          Return the native firmware health flag.
EARS           ( -- left-pos right-pos )                Return the current ears positions
ELSE           (immediate) ( -- )                       Begin the else part of a conditional Compile an unconditional jump to the matching THEN Update the previous conditional jump to point to the instruction after the ELSE
EMIT           ( char -- )                              Emit character
ENDCASE        (immediate) ( -- )                       End a case statement
ENDOF          (immediate) ( -- )                       End a case option
EVAL-TRAME     ( str -- )                               Evaluate a trame
EVALUATE       ( str -- )                               Evaluate a string as Forth code
EXECUTE        ( xt -- )                                Execute a word object previously obtained with '
EXIT           ( -- )                                   Return control to the caller
FALSE          ( -- false )
FREE-CELL      ( addr -- )                              Free a cell in memory at the given address
GC             ( -- mem% )                              Perform garbage collection and return the memory usage percentage
GET-HOUR       ( -- hour )                              Get the current hour
GET-MINUTE     ( -- minute )                            Get the current minute
HD             ( list -- list x )                       Get the head of a list
HELP           ( -- )
HERE           ( -- addr )                              Return the current data-space cell address
HTTP-GET       ( url -- content header )                Perform an HTTP GET request and return the content as a string HTTPS is not supported
HTTP-POST      ( payload url -- content header )        Perform an HTTP POST request and return the content as a string HTTPS is not supported
I              ( -- n )                                 Push the current loop index from the innermost DO...LOOP
IF             (immediate) ( cond -- )                  Begin a conditional Compile a conditional jump to the matching ELSE or THEN
IMMEDIATE      ( -- )                                   Mark the latest defined word as immediate
INTERPRETER    ( -- )                                   Interactive Forth interpreter
INVERT         ( x1 -- x2 )                             Invert all bits of x1, giving its logical inverse x2
IP             ( -- ip )                                Push the IP address of the WIFI interface onto the stack
J              ( -- n )                                 Push the current loop index from the next outer DO...LOOP
JMP            ( addr -- )                              Unconditional jump
JSON-GET       ( w path -- x ) (e.g., "daily.weathercode[0]") Get an element from a dictionary or list by path
JSON-PARSE     ( str -- list/dict )                     Parse a JSON string into a list or dictionary
LEAVE          (immediate) ( -- )                       Compile an early exit from the current DO...LOOP
LITERAL        (immediate) ( x -- )                     Compile x as a literal into the current colon definition
LOAD-INFO-ANIMATION ( filename anim_num -- )            Load info animation from the server
LOAD-INFO-ANIMATION ( filename anim_num -- )            Load info animation from the server
LOAD-INFO-ANIMATIONS ( -- )                             Load info animations from the server
LOAD-SRV       ( filename -- )                          Load a forth file from the server
LOCAL>STRING   ( -- str )                               Push the current local time and date as a string onto the stack
LOGIN          ( -- )                                   Login
LOOP           ( runtime ) ( loop-addr -- )             Increment loop index by 1 and repeat until the limit is reached
LOOP           (immediate) ( -- )                       End a counted loop with step +1
LOWER          ( s -- s )                               Convert string s to lowercase
LSHIFT         ( n1 n2 -- n1 << n2 )                    Left shift
MAC            ( -- mac )                               Push the MAC address of the WIFI interface onto the stack
MAX            ( n1 n2 -- max )                         Maximum of top two numbers
MD5            ( str -- hash )                          Compute the MD5 hash of a string
METEO>INFO     ( n -- n )                               Convert weather code to info code
MIN            ( n1 n2 -- min )                         Minimum of top two numbers
MOD            ( n1 n2 -- remainder )                   Modulus
MOVE-EAR       ( ear dir pos -- ) (0=left, 1=right) (0=left, 1=right) (0..16) Move an ear  ear in direction dir  to position pos
MS             ( delay -- )                             Sleep for the specified number of milliseconds
NEGATE         ( n -- -n )                              Negates the top number
NIL            ( -- list )                              Push empty list onto the stack
NIP            ( n1 n2 -- n2 )                          Drop the first item below the top of stack
NTH            ( list/str n -- x )                      Get the n-th element of a list or the n-th character of a string
OF             (immediate) ( value -- )                 Begin a case option
ON-CLICK       ( -- )                                   Click hook
ON-CONNECT     ( -- )
ON-DBL-CLICK   ( -- )                                   Double click hook
ON-EARS        ( -- )                                   Ears hook
ON-HALFTIME    ( -- )                                   Half-hour hook
ON-RFID        ( rfid -- )                              RFID hook
ON-TIME        ( -- )                                   Hour hook
OR             ( x1 x2 -- x3 )                          x3 is the bit-by-bit inclusive-or of x1 with x2
OVER           ( n1 n2 -- n1 n2 n1 )                    Make copy of second item on top
PAD_LEFT       ( s n c -- s )                           Pad string s on the left to length n with character c
PAD_RIGHT      ( s n c -- s )                           Pad string s on the right to length n with character c
PICK           ( n ... ni -- n ... ni ni )              Copy nth item to top of stack
PLAY-CHOR      ( chor_data -- )                         Play a choreography
PLAY-MIDI      ( i -- ) (i-100)                         Play a MIDI sound/note If i>=100, play note  If 0<=i<100, play sound i If i<0, play a random note
PLAY-URL       ( url -- )                               Play a WAV file from an HTTP URL HTTPS is not supported
POSTPONE       (immediate) ( "<spaces>name" -- )        Append compilation semantics of name to the current definition
PROMPT         ( -- )                                   Display the Forth interpreter prompt
R>             ( -- x ) ( R: x -- )                     Retrieve item from return stack
R@             ( -- x ) ( R: x -- x )                   Copy top of return stack onto data stack
RANDOM         ( max -- n )                             Return a pseudo-random number n between 0 and max-1
RDEPTH         ( -- count )                             Count number of items on return stack
RDROP          ( n --  )                                Discard top of return stack
READ-LINE      ( -- line )                              Read a line from the current socket
REBOOT         ( -- )                                   Perform a reboot
RECURSE        (immediate) ( -- )                       Compile a call to the current colon definition
RELOAD-INIT    ( -- )                                   Reload init.forth
REPEAT         (immediate) ( -- )                       End BEGIN ... WHILE loop
RESUME-TASK    ( task-id -- flag )                      Resume the task with the given task id.
REVISION       ( -- str )                               Push the revision string onto the stack
ROLL           ( n ... ni -- n ... ni ni )              Move nth item to top of stack
ROT            ( n1 n2 n3 -- n2 n3 n1 )                 Rotate third item to top
RSHIFT         ( n1 n2 -- n1 >> n2 )                    Right shift
SAY            ( text --  )                             Text to speech
SET-INFO-ANIMATION ( data n -- )                        Set info animation n from json
SLEEP          ( -- )                                   Put the Nabaztag into sleep mode
SLEEPING-TIME? ( -- flag )                              Check if current hour is in the sleep interval
SLEEPING?      ( -- flag)                               Return true if the Nabaztag is in sleep mode
SPACE          ( -- )                                   Display space
START-TASK     ( text delay name -- )                   Start a new task with the given name, delay, and text to execute.
STOP-SCRIPT-TASKS ( -- )                                Stop stale Forth script tasks after init reloads
STOP-TASK      ( task-id -- flag )                      Stop the task with the given task id.
STR-JOIN       ( list -- str )                          Join a list of strings into a single string
SURPRISE       ( -- )                                   Surprise
SURPRISE-TIME? ( -- flag )                              Check if it's time for a surprise
SUSPEND-TASK   ( task-id -- flag )                      Suspend the task with the given task id.
SWAP           ( n1 n2 -- n2 n1 )                       Exchange top two stack items
TAICHI         ( -- )                                   Do Tai Chi exercise
TAICHI-TIME?   ( -- flag )                              check if it's time for taichi
TCP-LISTEN     ( text port name -- )                    Start a TCP server listening on the given port with the given name
THEN           (immediate) ( -- )                       End a conditional Update the previous conditional jump or unconditional jump to point to the instruction after THEN
TIME&DATE      ( -- sec min hour day month year)        Push the current time and date onto the stack
TIME-MS        ( -- ms )                                Push the current time in milliseconds onto the stack
TIME?          ( -- flag )                              Check if the system time has been received from the time server
TL             ( list -- list' )                        Get the tail of a list
TOOL-MOVE_EARS ( args-json -- result )
TOOL-PLAY_SOUND ( args-json -- result )
TOOL-RABBIT_STATUS ( args-json -- result )
TOOL-SET_LED   ( args-json -- result )
TRUE           ( -- true )
TUCK           ( n1 n2 -- n1 n2 n1 ) (top)              Copy the first  stack item below the second stack item
T{             (immediate) ( --  )                      Records the current stack depth prior to the test code
T}             (immediate) ( --  )                      Compare the expected stack with the actual stack contents
UNLOOP         ( -- )                                   Remove the innermost DO...LOOP runtime frame
UNTIL          (immediate) ( x -- )                     End a loop Compile a conditional jump back to the matching BEGIN
UPDATE-TIME    ( -- )                                   Update the system time from the time server
UPDATE-WEATHER ( -- )                                   Update weather and air quality information
UPPER          ( s -- s )                               Convert string s to uppercase
UPTIME         ( -- s )                                 Push the system uptime in seconds onto the stack
URL-DECODE     ( s -- s )                               URL decode string s
URL-ENCODE     ( s -- s )                               URL encode string s
UTC>STRING     ( -- str )                               Push the current UTC time and date as a string onto the stack
VARIABLE       (immediate) ( -- addr )                  Define a variable and allocate a word for it
VOLUME         ( -- vol )                               Get the audio volume
WAKE-UP        ( -- )                                   Wake up the Nabaztag from sleep mode
WHILE          (immediate) ( x -- )                     Compile a conditional jump to the matching REPEAT
WORDS          ( -- )                                   List the words in the dictionary
XOR            ( x1 x2 -- x3 )                          x3 is the bit-by-bit exclusive-or of x1 with x2
[              (immediate) ( -- )                       Enter interpretation state during compilation
[']            (immediate) ( "<spaces>name" -- )        Compile the execution token of name as a literal
]              ( -- )                                   Return to compilation state

Variables
---------

CITY-CODE                                               City code (timezone)
CRONTAB-LAST-MINUTE                                     Crontab last checked minute
DST                                                     Daylight Saving Time
EAIQ                                                    European Air Quality Index
GO-TO-BED-AT                                            Go to bed at (hour)
INFO-DURATION                                           Info display duration (seconds)
INFO-MAIL                                               Info mail
INFO-MESSAGES                                           Info messages
INFO-PALETTE                                            Info LED palette (16 cells)
INFO-POLLUTION                                          Info pollution
INFO-SERVICE4                                           Info service 4
INFO-SERVICE5                                           Info service 5
INFO-STOCK                                              Info stock
INFO-TRAFFIC                                            Info traffic
INFO-WEATHER                                            Info weather
LANGUAGE                                                Language
LATITUDE                                                Latitude (weather/air quality)
LED-BASE                                                Base LED color override
LED-BASE-BREATHING                                      Base LED breathing effect
LED-CENTER                                              Center LED color override
LED-LEFT                                                Left LED color override
LED-NOSE                                                Nose LED color override
LED-RIGHT                                               Right LED color override
LONGITUDE                                               Longitude (weather/air quality)
MD5-PASSWORD                                            Login password (MD5 hashed)
NTP-SERVER                                              NTP server
SERVER-URL                                              Server URL
SURPRISE-NEXT-TIME                                      Next surprise time
TAICHI-FREQ                                             Tai Chi frequency
TAICHI-NEXT-TIME                                        Next taichi time
USERNAME                                                Login username
WAKE-UP-AT                                              Wake up at (hour)
WEATHER-CODE                                            Weather code
